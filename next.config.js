/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'unsplash.com',
      // Add other domains you might use
    ],
  },
}

module.exports = nextConfig